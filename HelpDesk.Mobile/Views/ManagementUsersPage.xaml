<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HelpDesk.Mobile.Views.ManagementUsersPage"
             xmlns:vm="clr-namespace:HelpDesk.Mobile.ViewModels"
             Title="Cadastro de Usuários"
             x:DataType="vm:ManagementUsersViewModel">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15" BackgroundColor="{StaticResource BackgroundColor}">

            <Label Text="Cadastrar Novo Usuário/Técnico" FontAttributes="Bold" FontSize="Large" TextColor="{StaticResource TextColor}" />

            <Entry Placeholder="Nome Completo" Text="{Binding Name}" />
            <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Senha" Text="{Binding Password}" IsPassword="True" />

            <Picker Title="Selecionar Perfil (Role)"
                    ItemsSource="{Binding Roles}"
                    SelectedItem="{Binding SelectedRole}" />

            <Button Text="Cadastrar"
                    Command="{Binding RegisterUserCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                    BackgroundColor="{StaticResource StatusClosedColor}"
                    Margin="0,20,0,0" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource PrimaryColor}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
